# API Documentation

PhotoConverter — это CLI утилита, которая не предоставляет HTTP API. 
Документ описывает внутренние интерфейсы и структуры данных.

## CLI Интерфейс

### Основная команда

```bash
photoconverter [flags]
```

### Флаги

| Флаг | Тип | Обязательный | По умолчанию | Описание |
|------|-----|--------------|--------------|----------|
| `--in` | string | да | - | Директория с исходными изображениями |
| `--out` | string | да | - | Директория для сохранения результатов |
| `--in-ext` | []string | нет | jpg,jpeg,png,heic,heif,webp,tiff | Расширения входных файлов |
| `--out-format` | string | нет | webp | Выходной формат (webp/jpg/png/avif/tiff/heic/jxl) |
| `--quality` | int | нет | 80 | Качество для lossy форматов (1-100) |
| `--workers` | int | нет | CPU cores | Количество параллельных воркеров |
| `--mode` | string | нет | skip | Режим работы (skip/dedup) |
| `--keep-tree` | bool | нет | true | Сохранять структуру директорий |
| `--strip` | bool | нет | false | Удалять метаданные из изображений |
| `--dry-run` | bool | нет | false | Симуляция без реальной конвертации |
| `--db` | string | нет | {out}/.photoconverter/state.sqlite | Путь к SQLite базе данных |
| `--vips-path` | string | нет | (автопоиск) | Путь к бинарнику vips |
| `-v, --verbose` | bool | нет | false | Подробный вывод |
| `--no-progress` | bool | нет | false | Отключить прогресс-бар |
| `--config` | string | нет | (автопоиск) | Путь к файлу конфигурации (YAML) |
| `--save-config` | string | нет | - | Сохранить настройки в YAML файл и выйти |
| `--max-width` | int | нет | 0 | Максимальная ширина изображения (0 = без ограничения) |
| `--max-height` | int | нет | 0 | Максимальная высота изображения (0 = без ограничения) |
| `--preset` | string | нет | - | Профиль качества (web/print/archive/thumbnail) |
| `--watch` | bool | нет | false | Режим слежения за директорией |
| `--save-preset` | string | нет | - | Сохранить настройки как именованный пресет |
| `--load-preset` | string | нет | - | Загрузить именованный пресет |

### Подкоманды

#### version

```bash
photoconverter version
```

Выводит версию и время сборки.

**Пример вывода:**
```text
photoconverter 1.0.0 (built 2024-01-15T10:30:00Z)
```

#### stats

```bash
photoconverter stats --db <path>
```

Показывает статистику из базы данных.

**Флаги:**
| Флаг | Тип | Обязательный | Описание |
|------|-----|--------------|----------|
| `--db` | string | да | Путь к SQLite базе данных |

**Пример вывода:**
```text
📊 Статистика базы данных:
   Всего записей: 1234
   Успешно: 1200
   Ошибок: 30
   В процессе: 4
```

## Схема базы данных SQLite

### Таблица `jobs`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | Первичный ключ (autoincrement) |
| `src_path` | TEXT | Абсолютный путь к исходному файлу |
| `src_size` | INTEGER | Размер исходного файла в байтах |
| `src_mtime` | INTEGER | Время модификации (unix timestamp) |
| `out_format` | TEXT | Выходной формат (webp, jpg, etc.) |
| `out_params` | TEXT | JSON с параметрами выхода |
| `out_params_hash` | TEXT | SHA256 хэш параметров |
| `content_sha256` | TEXT | SHA256 хэш содержимого (nullable) |
| `dst_path` | TEXT | Путь к выходному файлу |
| `status` | TEXT | Статус: in_progress, ok, failed |
| `error` | TEXT | Сообщение об ошибке |
| `started_at` | INTEGER | Время начала (unix timestamp) |
| `finished_at` | INTEGER | Время завершения (unix timestamp) |

### Индексы

```sql
-- Уникальный индекс для идемпотентности по источнику
CREATE UNIQUE INDEX ux_jobs_src
ON jobs (src_path, src_size, src_mtime, out_format, out_params_hash);

-- Уникальный индекс для дедупликации по содержимому
CREATE UNIQUE INDEX ux_jobs_dedup
ON jobs (content_sha256, out_format, out_params_hash)
WHERE content_sha256 IS NOT NULL AND status='ok';

-- Индекс для поиска по статусу
CREATE INDEX ix_jobs_status ON jobs (status);
```

### Примеры SQL-запросов

```sql
-- Получить все успешные задачи
SELECT src_path, dst_path FROM jobs WHERE status = 'ok';

-- Получить failed задачи с ошибками
SELECT src_path, error FROM jobs WHERE status = 'failed';

-- Статистика по форматам
SELECT out_format, COUNT(*) as count 
FROM jobs 
WHERE status = 'ok' 
GROUP BY out_format;

-- Найти дубликаты по содержимому
SELECT content_sha256, COUNT(*) as count, GROUP_CONCAT(src_path)
FROM jobs 
WHERE content_sha256 IS NOT NULL 
GROUP BY content_sha256 
HAVING count > 1;
```

## Внутренние структуры данных

### Config

```go
type Config struct {
    InputDir        string         // Директория с исходными изображениями
    OutputDir       string         // Директория для результатов
    InputExtensions []string       // Расширения входных файлов
    OutputFormat    OutputFormat   // Формат выходных файлов (webp/jpg/png/avif/tiff/heic/jxl)
    Quality         int            // Качество (1-100)
    Workers         int            // Количество воркеров
    DBPath          string         // Путь к SQLite
    Mode            Mode           // skip или dedup
    KeepTree        bool           // Сохранять структуру директорий
    DryRun          bool           // Симуляция
    VipsPath        string         // Путь к vips
    StripMetadata   bool           // Удалять метаданные
    Verbose         bool           // Подробный вывод
    NoProgress      bool           // Отключить прогресс-бар
}
```

### Job

```go
type Job struct {
    ID            int64     
    SrcPath       string    
    SrcSize       int64     
    SrcMtime      int64     
    OutFormat     string    
    OutParams     string    
    OutParamsHash string    
    ContentSHA256 *string   
    DstPath       *string   
    Status        JobStatus // in_progress, ok, failed
    Error         *string   
    StartedAt     *time.Time
    FinishedAt    *time.Time
}
```

### Stats

```go
type Stats struct {
    Processed int64  // Обработано
    Skipped   int64  // Пропущено
    Failed    int64  // С ошибками
    Total     int64  // Всего
}
```

## Формат параметров выхода (out_params)

JSON объект со всеми параметрами, влияющими на результат:

```json
{
    "format": "webp",
    "quality": 80,
    "strip_metadata": false
}
```

Хэш этого JSON используется для определения, изменились ли параметры конвертации.

## Коды возврата

| Код | Описание |
|-----|----------|
| 0 | Успешное завершение |
| 1 | Ошибка (есть failed задачи или ошибка конфигурации) |

## Переменные окружения

| Переменная | Описание |
|------------|----------|
| `PHOTOCONVERTER_VIPS` | Путь к бинарнику vips |

## Примеры использования

### Базовая конвертация

```bash
photoconverter --in ./photos --out ./converted --out-format webp
```

**Вывод:**

```text
📦 Найден vips: /usr/bin/vips (версия 8.14.2)
🚀 Запуск конвертации:
   Вход: ./photos
   Выход: ./converted
   Формат: webp (качество: 80)
   Режим: skip
   Воркеров: 8

🔄 Конвертация [████████████████████] 150/150 (12.5 файл/с)

📊 Результаты:
   Обработано: 150
   Пропущено: 0
   Ошибок: 0
   Время: 12.5s
```

### Повторный запуск (идемпотентность)

```bash
photoconverter --in ./photos --out ./converted --out-format webp
```

**Вывод:**

```text
📦 Найден vips: /usr/bin/vips (версия 8.14.2)
🚀 Запуск конвертации:
   Вход: ./photos
   Выход: ./converted
   Формат: webp (качество: 80)
   Режим: skip
   Воркеров: 8

🔄 Конвертация [████████████████████] 150/150 (500.0 файл/с)

📊 Результаты:
   Обработано: 0
   Пропущено: 150
   Ошибок: 0
   Время: 0.3s
```

### Режим дедупликации

```bash
photoconverter --in ./photos --out ./unique --mode dedup -v
```

**Вывод (verbose):**

```text
📦 Найден vips: /usr/bin/vips (версия 8.14.2)
📁 Найдено файлов для обработки: 200
🚀 Запуск конвертации:
   Вход: ./photos
   Выход: ./unique
   Формат: webp (качество: 80)
   Режим: dedup
   Воркеров: 8

✅ photo1.jpg -> unique/photo1.webp (0.15s)
✅ photo2.jpg -> unique/photo2.webp (0.12s)
⏭️  Пропущен: photo3.jpg (дубликат по содержимому)
...

📊 Результаты:
   Обработано: 120
   Пропущено: 80
   Ошибок: 0
   Время: 8.2s
```
